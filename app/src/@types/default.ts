export enum buttonComponentColor {
  PRIMARY = "primary",
  SECONDARY = "secondary",
  SUCCESS = "success",
  WARNING = "warning",
  ERROR = "error",
  INFO = "info",
  NEUTRAL = "neutral",
  NONE = "none",
}

export enum buttonComponentType {
  INLINE = 'inline',
  OUTLINE = 'outline',
}

export enum buttonComponentSize {
  SMALL = 'small',
  MEDIUM = 'medium',
  LARGE = 'large',
}
